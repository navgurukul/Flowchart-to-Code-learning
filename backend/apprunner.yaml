version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip3 install -r requirements.txt

run:
  env:
    - name: GEMINI_API_KEY
      value: "AIzaSyCAdv_yJyCFUX38SfQmMEzRJrIubaM-GRU"
  pre-run:
    - pip3 install -r requirements.txt
  command: uvicorn main:app --host 0.0.0.0 --port 8080
